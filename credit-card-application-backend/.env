# Use local MongoDB (default). Start MongoDB locally or install from https://www.mongodb.com/try/download/community
MONGO_URI=mongodb://localhost:27017/creditcard

# Optional: MongoDB Atlas (uncomment and fix if needed)
# - Ensure cluster exists and you have network access
# - URL-encode special characters in password (e.g. > as %3E, so >> becomes %3E%3E)
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/DATABASE?retryWrites=true&w=majority